rules_version = '2';
// Deny-all until KB 21-spec-security-rules.md ships (phase I). The client SDK holds no
// rules today, so this is the current effective behaviour, now versioned.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} { allow read, write: if false; }
  }
}
